name: Deploy to cPanel - Assets

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd /home/dxtrmfwa/repositories/inventory-app
            git pull origin main

            echo "📦 Installing frontend dependencies..."
            npm install --legacy-peer-deps

            echo "🔨 Building frontend..."
            npm run build

            echo "📂 Copying frontend build to production..."
            rm -rf /home/dxtrmfwa/public_html/assets.mfwa.org/dist
            cp -r dist /home/dxtrmfwa/public_html/assets.mfwa.org/

            echo "📂 Copying backend files to production..."
            cp app.js /home/dxtrmfwa/public_html/assets.mfwa.org/
            cp cron-auto-close.js /home/dxtrmfwa/public_html/assets.mfwa.org/
            cp emailService-resend.js /home/dxtrmfwa/public_html/assets.mfwa.org/
            cp inject-env.js /home/dxtrmfwa/public_html/assets.mfwa.org/
            
            # 🔑 IMPORTANT: Copier les routes ET les services
            cp -r server/routes /home/dxtrmfwa/public_html/assets.mfwa.org/
            cp -r server/services /home/dxtrmfwa/public_html/assets.mfwa.org/server/

            echo "📦 Installing backend dependencies..."
            cd /home/dxtrmfwa/public_html/assets.mfwa.org
            npm ci --omit=dev

            echo "🔄 Restarting PM2..."
            pm2 restart assets-mgt

            echo "✅ Deployment complete!"